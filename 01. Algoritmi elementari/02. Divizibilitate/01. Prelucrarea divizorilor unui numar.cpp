// complexitate: O(n)
  for (int i = 1; i <= n; ++ i)
      if (n % i == 0)
          // prelucrare i
          
          
// complexitate: O(sqrt{n})
  int i = 1;
  for (i = 1; i * i < n; ++ i)
      if (n % i == 0)
          // prelucrare i si n / i
  if (i * i == n) {
      // n este patrat perfect
      // prelucrare i
  }
